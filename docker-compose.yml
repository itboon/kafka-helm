version: "3"

volumes:
  kafka-data: {}

services:
  kafka:
    image: kafkace/kafka:v3.9.0
    restart: always
    network_mode: "host"
    volumes:
      - kafka-data:/var/lib/kafka/data
    environment:
      KAFKA_HEAP_OPTS: -Xmx1024m -Xms1024m